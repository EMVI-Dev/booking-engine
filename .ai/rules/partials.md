---
paths:
  - resources/views/storefront/partials/navbar.blade.php
---

# Partials

## Active nav uses brand fill
Active guest nav pills use bg-brand-600 text-brand-foreground (same as CTAs), not a subtle elevated zinc/white chip with brand-800 text. Idle stays muted slate.

## Mobile drawer has no WhatsApp chat
WhatsApp chat stays in the sticky topbar (and desktop floating button). Do not duplicate Chat with operator inside the mobile nav drawer.
